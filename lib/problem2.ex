defmodule Advent2019.Problem2 do
  def solve(program) do
    new_program = execute(program, )
  end

  def execute(program, [99 | rest]) do
    program
  end
end

# index = 0
# while index < len(program)
#   command = program.slice(index, index + 4)
#   program = execute(program, command)
